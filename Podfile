# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

use_frameworks!
target 'SpreadsheetViewDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  

  #pod 'SpreadsheetView'

end
